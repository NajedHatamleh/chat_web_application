<%= simple_form_for @room do |form| %>
  <%= form.input :name, required: true, minLength: 1 %>
  <%= form.submit "Save", class: 'btn btn-success' %>
  <%= link_to 'Back', rooms_path, class: "btn btn-warning" %>
<% end %>
